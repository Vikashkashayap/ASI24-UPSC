/**
 * Document model alias → SourceUrl (website chapter or uploaded PDF).
 * Keeps product vocabulary (Document) aligned with the shared Knowledge Base.
 */
export { default } from "../../models/SourceUrl.js";
