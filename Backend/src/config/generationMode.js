/**
 * Question generation mode.
 * TEMP: Admin KB + RAG retrieval is off everywhere for MCQ generation.
 * Re-enable later by setting SKIP_KB_RAG_RETRIEVAL = false.
 */
export const SKIP_KB_RAG_RETRIEVAL = true;
