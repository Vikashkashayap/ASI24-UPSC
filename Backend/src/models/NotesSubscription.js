/**
 * Backward-compatible re-export.
 * Canonical model lives in Subscription.js (collection: subscriptions).
 */
export { Subscription as NotesSubscription, Subscription } from "./Subscription.js";
